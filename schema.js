const GraphQLObjectType = require('graphql');


// Alot of different types with graphql
// e.g. Object Type 

const LaunchType = new GraphQLObjectType({
    name: 'Launch',
    fields: () => ({
        
    })
});