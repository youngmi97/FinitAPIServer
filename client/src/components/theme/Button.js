import styled from "styled-components";

const Button = styled.button`
  background: transparent;
  border-radius: 4px;
  color: ${(props) => props.theme.SurplusPurple};
  padding: 28px 10px;
`;

export default Button;
