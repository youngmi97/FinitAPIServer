import React from "react";

import { Card, CardContent } from "material-ui/core";
import { classList } from "utils";

const useStyles = makeStyles((theme) => ({
  root: {
    minWidth: 217,
  },
}));
const SubscriptionCard = { title, subtitle, icon, context };
